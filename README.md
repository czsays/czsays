### CZ

**Product manager, escaped containment.** Boston.
Once described as "a precision bull in a china shop."

I build the connective tissue that turns "an LLM" into something that actually does the work — not just talks about it. Quietly the most interesting layer in the stack, and I write up the reusable parts so the next person doesn't have to reverse-engineer them the way I did.

**What I build**
- 🚦 **Agent infrastructure** — dispatch, routing, memory, and the connectors that wire agents into real tools
- 🧩 **MCP servers** — the tool surfaces the official ones skip
- 🪄 **Personal automation** — anything in my week that shouldn't be done by hand, built away; plus a homelab that's really a playground for persistent agents, and an Obsidian vault I've over-engineered into a second brain

**A few that escaped into public**
- [agent-dispatch-kit](https://github.com/czsays/agent-dispatch-kit) — signed webhook in, the right agent backend out
- [linear-extras-mcp](https://github.com/czsays/linear-extras-mcp) — the Linear MCP tools the official server skipped
- [agent-connector-kit](https://github.com/czsays/agent-connector-kit) — reference connectors for wiring agents into the real world
- [spire-slayer](https://github.com/czsays/spire-slayer) — because someone had to compute the optimal Slay the Spire line, and it wasn't going to be me

Yes, GitHub is flooded with this exact kind of thing right now. I'm less attached to the artifacts than to what building them sharpens — on a frontier this fast, knowing which parts are worth keeping is the actual job. I write about that as I go.
